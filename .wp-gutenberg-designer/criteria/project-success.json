{
  "schemaVersion": 1,
  "success": [
    "Project profile exists and validates.",
    "Generated templates use WordPress-native block markup.",
    "Visual system uses theme.json tokens.",
    "Quality loop produces a pass, risk, or blocked report with evidence."
  ],
  "qualityGates": {
    "runtime": [
      "npm run verify returns ok against http://100.70.222.25:8093",
      "WordPress core version is readable from wordpress/wp-includes/version.php",
      "No docker compose file or Dockerfile is required for boot"
    ],
    "gutenberg": [
      "Prefer core blocks, theme.json, templates, template parts, and patterns before custom blocks",
      "Run editor compatibility checks before publishing generated block markup"
    ],
    "performance": {
      "seo": "Crawlable text, one H1 per page type, image alt intent required",
      "accessibility": "Logical headings and no nested interactive controls",
      "target": "Keep custom CSS small and token-based until design system is confirmed"
    }
  }
}
