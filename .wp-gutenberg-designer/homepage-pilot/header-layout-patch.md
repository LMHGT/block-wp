# Header Layout Patch

The live browser showed the header CTA wrapping to a second line after theme CSS began loading through theme.json. The header now uses a four-column desktop grid: site title, tagline, navigation, CTA. Smaller viewports collapse to two rows and then one column.
