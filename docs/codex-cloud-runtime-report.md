# Codex Cloud WordPress Runtime Report

Date: not run

WordPress base URL: not configured

This report is the active runtime proof target for the LMHG WordPress transition.
RackNerd and local WordPress/Docker are not accepted proof surfaces for this
project.

Run:

```bash
CODEX_CLOUD_WP_URL="https://<codex-cloud-wordpress-url>" npm run cloud:verify
```
